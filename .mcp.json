{
  "mcpServers": {
    "nixos": {
      "command": "nix",
      "args": [
        "--extra-experimental-features",
        "nix-command flakes",
        "run",
        "github:utensils/mcp-nixos",
        "--"
      ]
    }
  }
}
